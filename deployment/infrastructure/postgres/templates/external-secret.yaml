apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-secrets
  namespace: {{ .Values.global.namespace }}
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: teck-website-vault
    kind: ClusterSecretStore
  target:
    name: postgres-secret
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: {{ .Values.global.vaultPath }}/postgres
        property: username
    - secretKey: password
      remoteRef:
        key: {{ .Values.global.vaultPath }}/postgres
        property: password
