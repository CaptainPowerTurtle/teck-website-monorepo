name: Manual Vault Preview Cleanup

description: Manually delete all secrets and metadata under a specified preview namespace in Vault.

on:
  workflow_dispatch:
    inputs:
      namespace:
        description: 'Preview namespace to delete (e.g. preview-123)'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  manual-vault-preview-cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Vault CLI
        run: |
          curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -
          sudo apt-add-repository "deb [arch=amd64] https://apt.releases.hashicorp.com $(lsb_release -cs) main"
          sudo apt-get update && sudo apt-get install vault
      
      - name: Setup Vault with JWT
        uses: hashicorp/vault-action@v3
        with:
          url: ${{ secrets.VAULT_ADDR }}
          method: jwt
          role: github-actions
          exportToken: true

      - name: Print Vault Token and Accessor
        run: |
          echo "VAULT_TOKEN=$VAULT_TOKEN"
          vault token lookup | grep accessor
        env:
          VAULT_TOKEN: ${{ env.VAULT_TOKEN }}
          VAULT_ADDR: ${{ secrets.VAULT_ADDR }}

      - name: Delete Preview Secrets (Manual)
        run: |
          NAMESPACE="${{ github.event.inputs.namespace }}"
          echo "Cleaning up all preview secrets for: teck-website/data/${NAMESPACE}"
          echo "Using Vault server: ${VAULT_ADDR}"

          # Recursively destroy all secrets and subfolders (all versions + metadata) using full path (mount + secret path)
          delete_recursive() {
            local path="$1"
            echo "Listing items at: teck-website/${path}"
            local items=$(vault kv list "teck-website/${path}" 2>/dev/null | tail -n +3)

            # Always attempt to delete direct secret at this path
            echo "Attempting to delete direct secret: teck-website/${path}"
            echo "Before deletion, kv get output:"
            vault kv get "teck-website/${path}" || echo "No secret found at teck-website/${path}"
            echo "Destroying all versions of secret: teck-website/${path}"
            vault kv destroy "teck-website/${path}" || true
            echo "Deleting secret metadata: teck-website/${path}"
            vault kv metadata delete "teck-website/${path}" || true
            echo "After deletion, kv get output:"
            vault kv get "teck-website/${path}" || echo "No secret found at teck-website/${path}"

            if [ -z "$items" ]; then
              return
            fi
            for item in $items; do
              item="${item%/}"
              echo "Found item: $item"
              # Always recurse into subpath, even if kv list does not show it as a folder
              delete_recursive "${path}/${item}"
            done
          }

          delete_recursive "$NAMESPACE"
          echo "Cleaned up entire preview folder: teck-website/${NAMESPACE}"
        env:
          VAULT_ADDR: ${{ secrets.VAULT_ADDR }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
